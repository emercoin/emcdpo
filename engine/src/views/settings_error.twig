<!DOCTYPE html>
<html>
<head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=0.85, maximum-scale=0.85, user-scalable=0">
        <title>EMC DPO Manager</title>
        <link rel="stylesheet" type="text/css" href="{{ global.request.basepath }}/style.css">
		<link rel="stylesheet" type="text/css" href="{{ global.request.basepath }}/css/cs-select.css">
		<link rel="stylesheet" type="text/css" href="{{ global.request.basepath }}/css/cs-skin-underline.css">
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
</head>
<body class="yellow-bg">
<select class="cs-select cs-skin-underline">
	<option value="" disabled selected>{{ global.session.get('locale')|upper }}</option>
	{% for locale in LOCALES %}
		<option data-link="{{ path('setlocale', {'locale': locale}) }}" value="locale">{{ locale|upper }}</option>
	{% endfor %}
</select>
	<div class="one-column">
		<div class="logo logo-black"></div>
		<h1 class="black upper">Emercoin</h1>
		<h3 class="black upper">{{ 'index.head.digital_proof_of_ownership'|trans }}</h3>
		<div class="error black">
			<h1><i class="material-icons">warning</i>{{ 'settings_error.head'|trans }}</h1>
			<span>{{ 'settings_error.message'|trans }} <strong>%DOCUMENT_ROOT%/engine/src/settings.php</strong></span>
		</div>
	</div>
<script type="text/javascript" src="{{ global.request.basepath }}/js/classie.js"></script>
<script type="text/javascript" src="{{ global.request.basepath }}/js/selectFx.js"></script>
<script>
	(function() {
		[].slice.call( document.querySelectorAll( 'select.cs-select' ) ).forEach( function(el) {
			new SelectFx(el, {newTab: false});
		} );
	})();
</script>
</body>
</html>
